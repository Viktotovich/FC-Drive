<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%=title%></title>
    <link rel="stylesheet" href="/css/styles.css" />
  </head>
  <body>
    <%-include("../partials/header")%>
    <section class="mx-20">
      <h1 class="mt-5 mb-10 text-3xl text-center"><%=title%></h1>
      <form action="/folder/rename/<%=folderName%>/<%=folderId%>" method="POST">
        <fieldset class="border-slate-500 flex flex-col items-center border-1">
          <legend class="p-1 ml-4">Change folder name:</legend>
          <label for="folderName">Enter a new name: </label>
          <input
            type="text"
            minlength="2"
            maxlength="50"
            required
            id="folderName"
            name="folderName"
            value="<%=folderName%>"
            class="mb-3 mt-1 pl-1 rounded-sm border-sky-900 border-1"
          />
          <button
            type="submit"
            class="bg-blue-600 text-white hover:bg-blue-500 py-1 px-7 rounded-sm mt-2 mb-7 hover:cursor-pointer"
          >
            Save Change
          </button>
        </fieldset>
      </form>
    </section>
    <%-include("../partials/footer")%>
  </body>
</html>
