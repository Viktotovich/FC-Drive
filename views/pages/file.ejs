<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%=title%></title>
    <link rel="stylesheet" href="/css/styles.css" />
  </head>
  <body>
    <%-include("../partials/header")%>
    <section class="mx-20">
      <h1 class="mt-5 mb-10 text-3xl text-center"><%=title%></h1>
      <%-include("../partials/file-breakdown")%>
      <form
        action="/file/<%=file.name%>/<%=file.id%>/delete"
        method="POST"
        class="mb-20 grid justify-center"
      >
        <button
          type="submit"
          class="px-14 py-2 rounded-sm border-slate-100 hover:bg-slate-100 hover:border-slate-200 border-2 hover:cursor-pointer"
        >
          Delete
        </button>
        <p class="text-xs">*This action will be irreversable</p>
      </form>
    </section>
    <%-include("../partials/footer")%>
  </body>
</html>
