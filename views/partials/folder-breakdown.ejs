<%if (folderData.files !== undefined && folderData.files.length > 0) { %>
<%folderData.files.forEach((file) => { %>
<div
  class="rounded-sm hover:bg-amber-100 border-slate-200 hover:border-amber-200 border-1"
>
  <a href="/file/<%=file.name%>/<%=file.id%>" class="flex">
    <span class="text-xl py-1 ml-3">📂</span>
    <p class="ml-10 grid content-center"><%=file.name%></p>
  </a>
</div>
<%})%> <%} else { %>
<div
  class="rounded-sm hover:bg-slate-50 border-slate-200 hover:border-slate-50 border-1"
>
  <p class="py-2 grid justify-center">You have no files yet</p>
</div>
<%}%>
